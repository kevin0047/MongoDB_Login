<Window x:Class="MongoDB_Login.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login/Register" Height="300" Width="300">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Username:"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Password:"/>
            <PasswordBox x:Name="PasswordBox"/>
            <Button Content="Login" Command="{Binding LoginCommand}" Margin="0,10,0,0"/>
            <Button Content="Register" Command="{Binding RegisterCommand}" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding Message}" Margin="0,10,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>